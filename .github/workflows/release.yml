name: Release (tag sémantique)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Installer les dépendances
        run: pip install pyinstaller fpdf2 tkcalendar

      - name: Injecter la version
        run: Set-Content version.py "__version__ = '${{ github.ref_name }}'"

      - name: Construire l'exe
        run: |
          pyinstaller --onefile --windowed --name EPADE `
            --add-data "assets/fonts;assets/fonts" `
            --icon "assets/epade.ico" `
            main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: EPADE-windows
          path: dist/EPADE.exe

  build-linux:
    runs-on: ubuntu-latest
    container: rockylinux/rockylinux:9
    steps:
      - name: Installer git
        run: dnf install -y git

      - uses: actions/checkout@v6

      - name: Installer Python 3.12 et tkinter
        run: dnf install -y python3.12 python3.12-pip python3.12-tkinter

      - name: Installer les dépendances Python
        run: python3.12 -m pip install --upgrade pip && python3.12 -m pip install pyinstaller fpdf2 tkcalendar

      - name: Injecter la version
        run: echo "__version__ = '${{ github.ref_name }}'" > version.py

      - name: Construire le binaire
        run: |
          python3.12 -m PyInstaller --onefile --windowed --name EPADE \
            --add-data "assets/fonts:assets/fonts" \
            --icon "assets/epade.ico" \
            main.py

      - name: Créer l'archive tar.gz
        run: |
          mkdir -p EPADE-linux
          cp dist/EPADE EPADE-linux/EPADE
          cp assets/epade.png EPADE-linux/epade.png
          cp install-bin.sh EPADE-linux/install.sh
          chmod +x EPADE-linux/EPADE EPADE-linux/install.sh
          tar -czf EPADE-linux-x86_64.tar.gz EPADE-linux/

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: EPADE-linux
          path: EPADE-linux-x86_64.tar.gz

  release:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Télécharger les artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist

      - name: Renommer les binaires
        run: |
          mv dist/EPADE-windows/EPADE.exe dist/EPADE.exe
          mv dist/EPADE-linux/EPADE-linux-x86_64.tar.gz dist/EPADE-linux-x86_64.tar.gz

      - name: Créer la release GitHub
        uses: softprops/action-gh-release@v3
        with:
          name: "ÉPADE ${{ github.ref_name }}"
          body: |
            ## ÉPADE ${{ github.ref_name }}

            ### Installation Windows
            1. Télécharger `EPADE.exe`
            2. Double-cliquer pour lancer — aucune installation requise
            3. La base de données `data\epade.db` est créée automatiquement à côté de l'exe

            ### Installation Linux
            1. Télécharger et extraire `EPADE-linux-x86_64.tar.gz`
            2. Placer le dossier `EPADE-linux/` à l'emplacement souhaité (local ou dossier réseau partagé)
            3. Lancer `install.sh` une fois par poste pour installer l'icône et le lanceur
            4. Lancer directement : `./EPADE` depuis le dossier
            5. La base de données `data/epade.db` est créée automatiquement dans le dossier

            ### Installation Linux (depuis les sources) / macOS
            Voir [README.md](https://github.com/${{ github.repository }}/blob/master/README.md)
          generate_release_notes: true
          files: |
            dist/EPADE.exe
            dist/EPADE-linux-x86_64.tar.gz
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
